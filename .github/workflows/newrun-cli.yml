name: New Run MOe AI Agent

on:
  workflow_dispatch:  # Manual trigger from GitHub UI

jobs:
  run-cli-agent:
    name: Run CLI Chat Mode
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
    python -m pip install --upgrade pip
          pip install -r pip install \
  openai>=1.12.0 \
  anthropic>=0.18.0 \
  python-dotenv>=1.0.0 \
  click>=8.1.0 \
  rich>=13.7.0 \
  gitpython>=3.1.40 \
  tiktoken>=0.6.0 \
  aiohttp>=3.9.0 \
  aiofiles>=23.2.0 \
  pydantic>=2.5.0 \
  PyYAML>=6.0.1 \
  tree-sitter>=0.20.4 \
  tree-sitter-languages>=1.9.1 \
  pathspec>=0.12.1 \
  watchdog>=4.0.0 \
  jinja2>=3.1.3 \
  black>=24.0.0 \
  isort>=5.13.0 \
  flake8>=7.0.0 \
  mypy>=1.8.0 \
  pytest>=8.0.0

          

      - name: Run AI Agent CLI in Chat Mode
        run: python -m ai_coding_agent cl
